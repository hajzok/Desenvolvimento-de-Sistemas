import { StyleSheet } from 'react-native';

const estilo = StyleSheet.create({
  boxEstados:{
    padding: 7,
    borderWidth: 7,
    borderColor: '#D2691E',
    borderRadius: 30,
    marginBottom: 8
  },
  nomeEstados: {
    fontSize: 15,
    marginBottom: 5
  }
});

export default estilo;