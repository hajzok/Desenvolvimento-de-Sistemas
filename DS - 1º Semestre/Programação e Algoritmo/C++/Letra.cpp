#include<stdio.h>
#include<locale.h>

int main()
{
	char letra, vc;
	
	printf("Digite a letra: ");
	scanf("%c",letra);
	
	if (letra=a && e && i && o && u)
	{
		vc=vogal;
	}
	else if
	{
		vc=consoante;
	}
	printf("A letra é %.c", vc);
return 0
}
